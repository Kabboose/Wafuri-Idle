/** Temporary client-side defaults for the current run intent payload. */
export const GAMEPLAY_CONFIG = {
  run: {
    defaultSpeed: 1,
    defaultCritChance: 2_500,
    defaultDurationMs: 10_000
  }
} as const;
